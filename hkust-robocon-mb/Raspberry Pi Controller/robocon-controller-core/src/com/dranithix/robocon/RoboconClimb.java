package com.dranithix.robocon;

/**
 * 
 * @author Christopher PK & Kenta Iwasaki
 *
 */
public class RoboconClimb {

	private static final int CLIMB_UP = 1799;
	private static final int CLIMB_DOWN = -600;

	public static void climpUp() {
		// TODO: motor_set_vel(MOTOR4, CLIMB_UP, OPEN_LOOP);
		// TODO: motor_set_vel(MOTOR5, CLIMB_UP, OPEN_LOOP);
		// TODO: motor_set_vel(MOTOR6, CLIMB_UP, OPEN_LOOP);
	}

	public static void climbDown() {
		// TODO: motor_set_vel(MOTOR4, CLIMB_DOWN, OPEN_LOOP);
		// TODO: motor_set_vel(MOTOR5, CLIMB_DOWN, OPEN_LOOP);
		// TODO: motor_set_vel(MOTOR6, CLIMB_DOWN, OPEN_LOOP);
	}

	public static void climbStop() {
		// TODO: motor_set_vel(MOTOR4, 0, OPEN_LOOP);
		// TODO: motor_set_vel(MOTOR5, 0, OPEN_LOOP);
		// TODO: motor_set_vel(MOTOR6, 0, OPEN_LOOP);
	}

}
